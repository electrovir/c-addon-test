{
  "name": "c-addon-test",
  "scripts": {
    "compile": "npm install && node-gyp build",
    "test": "node index.js"
  },
  "main": "index.js",
  "author": "electrovir",
  "private": true,
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^2.0.0",
    "node-gyp": "^6.0.1"
  }
}
